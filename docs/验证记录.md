# 验证记录

## 交付结果

- 日期：2026-09-19。
- 最终版本：1.0.4，versionCode 5。
- 交付文件：`release/TVMinesweeper-release-1.0.4.apk`。
- Java / 原生 Android Framework；minSdk 18、targetSdk 28、compileSdk 28。
- Release 已执行 R8 / ProGuard 代码压缩、资源压缩，并通过 v1 / v2 签名验证。
- APK 只有一个 DEX，不包含 `lib/` 原生库、`.so`、网络权限或其他权限。没有额外构建 x86 / arm32 版本。

## 自动化检查

| 检查 | 结果 |
| --- | --- |
| GameEngine JUnit | 17 项通过，0 failure / error |
| DoubleClickDetector JUnit | 9 项通过，0 failure / error |
| release Lint | 0 Error；12 项非阻断 Warning |
| release 混淆 / 资源压缩 / 签名打包 | 成功 |
| APK 签名 | v1、v2 验证通过 |
| Markdown 换行 | 全部 LF |

JUnit 覆盖三档布雷、首次周围安全、迭代展开、旗帜限制、胜利、失败、数字快开、错误旗导致踩雷、存档往返、损坏存档拒绝，以及单击、双击、260ms / 280ms 边界、超时、取消、三连击冷却等情况。

Lint Warning 为 TV banner 的 API 提示、英文复数建议、未使用字符串、图标形状建议和程序化 View 构造函数提示；未关闭 Error 检查。未使用资源由 release 资源压缩移除。

另外在临时测试环境验证 13 项积分边界、37 项仓库存储 / 音效生命周期契约。该部分使用替身 SharedPreferences / SoundPool 和 JVM JSON 实现，属于契约验证，不替代 Android 真机音频体验检查；临时产物位于 `E:\temp\TVMinesweeper\data-check`。

## 雷电模拟器验证

设备：`emulator-5554`，Android 9 / API 28，物理显示 1080×1920，游戏横屏实际为 1920×1080。纯 Java release APK 安装、覆盖升级、冷启动均成功。

`tools/verify-device.ps1` 在大棋盘版本完成 34 项断言：

- 初级左上角与上、左边界保持；中级 16×16、高级 30×16 的右下边界保持。
- OK 双击只插旗，不触发翻开；单击安全翻开并开始计时。
- 音量 + / − 插旗和取消旗，长按只执行一次。
- MENU 打开 / 关闭，方向选择，返回取消，取消后保留棋盘和光标。
- 菜单期间暂停计时，关闭菜单后继续。
- 应用进入后台后暂停，返回显示暂停遮罩；OK 只继续，不额外翻格。
- 带已开格和旗帜的对局正常保存；重新启动后恢复棋盘、光标、旗帜、累计时间。
- 测试结束恢复初级空白新局。

34 项完整回归在 1.0.3 执行；最终 1.0.4 仅修正菜单描边裁切及较长计时文本适配，并重新执行全部 JUnit、Lint、release 构建、覆盖安装和对应界面复验。文本回归报告为 `E:\temp\TVMinesweeper\device-report-20260919-184033.txt`。

另补测音效关闭 / 开启、内置玩法帮助、退出确认和默认继续，均通过。最终应用进程未发现 AndroidRuntime 崩溃或 TVMinesweeper 警告日志。

## 大屏布局

时间、雷数、难度和操作提示移至排行榜旁的窄信息侧栏。棋盘优先占用可用高度，1080p 下初级、中级、高级格子约为 99.6、56、42.7 像素，数字字号相应扩大。

实际检查 1080p 游戏画面及 720p 高级棋盘、难度菜单。720p 检查通过后已恢复模拟器原分辨率。截图直接读取 Android framebuffer，不激活或遮挡电脑上其他窗口；先执行本地 Windows OCR，再压缩为 JPEG，仓库仅保留压缩图：

- [初级游戏画面](../screenshots/gameplay.jpg)
- [720p 高级棋盘](../screenshots/expert-720.jpg)
- [720p 难度菜单](../screenshots/menu-720.jpg)

## 验证范围

API 18 兼容性通过构建配置、兼容 API 用法和 Lint 检查；本次实际运行设备为 API 28，未连接 Android 4.3 实体电视或 arm64 实体电视。不同电视的红外音量键是否传入应用，需在对应遥控器验证；OK 双击插旗始终作为不依赖音量键的操作方式。
