# 验证记录

## 交付结果

- 日期：2026-09-21。
- 版本：1.0.8，versionCode 9。
- 文件：`release/TVMinesweeper-release-1.0.8.apk`，129476 字节。
- Java / 原生 Android Framework，minSdk 18、targetSdk 28、compileSdk 28。
- release 的 R8、资源压缩及 v1 / v2 签名验证通过。
- APK 为 1 个 DEX、0 个原生库、无权限声明，可用于 arm64 设备；未额外构建 x86 / arm32 版本。

## 五档配置与布局

| 难度 | 列 × 行 | 雷数范围 | 1080p 新局单格边长约 |
| --- | ---: | ---: | ---: |
| 初级 | 9 × 9 | 10–15 | 113.3px |
| 中级 | 12 × 10 | 18–24 | 102px |
| 高级 | 16 × 13 | 34–44 | 78.5px |
| 困难 | 20 × 16 | 58–72 | 63.75px |
| 挑战 | 24 × 19 | 90–108 | 53.7px |

五档新局都以方格铺满约 1020px 的棋盘可用高度，继续保留实际 2px 外边距和底部状态行。旧高级 30×16 棋盘恢复后保留原布局，格子单独计算宽高，约 44.1×63.75px，消除原先上下留白。图标按较短边绘制；光标边框和触摸分别使用实际宽高。

## 自动化检查

| 检查 | 结果 |
| --- | --- |
| 游戏引擎 JUnit | 22 项通过 |
| TVM1 / TVM2 / TVM3 迁移 JUnit | 6 项通过 |
| 排行榜、积分与迁移 JUnit | 10 项通过 |
| 设置与光标存档 JUnit | 9 项通过 |
| 双击检测 JUnit | 9 项通过 |
| 按键路由 JUnit | 12 项通过 |
| release Lint | 0 Error、7 项非阻断 Warning |
| Markdown | 全部 LF |

总计 68 项 JUnit 全部通过。覆盖五档范围端点、首步安全、胜负、数字快开、旧局逐格保留及继续游戏、序列化反复恢复、非法布局和坏档拒绝、排行榜只迁移一次、原分数与日期保留、写入失败后重试，以及挑战档第 19 行第 24 列光标保存。

数据迁移测试使用测试专用 SharedPreferences 替身和真实 JSON 库；这些仅在 JVM 测试中使用，不包含在 APK 中。Lint 提示为旧 API 的 TV banner/内联常量、英文复数、图标形状及程序化 View 构造函数建议，未关闭 Error 检查。

## 覆盖升级

在雷电模拟器上从 1.0.7 覆盖安装 1.0.8，没有清除数据。升级前后的旧高级真实对局保持一致：30×16、108 雷、已开 12 格、1 面旗帜、光标第 16 行第 30 列、累计 1 秒、暂停状态。新版本将此局映射为挑战档，保留原布局；下一次开局才采用 24×19 新规格。

本地像素检查确认旧宽棋盘在 y=30 与 y=1010 均有格子填充；点击新拉高区域的底行得到第 16 行第 1 列，验证触摸与绘制坐标一致。升级前后无障碍 XML 保存于 `E:\temp\TVMinesweeper\five-upgrade-before.xml` 和 `five-upgrade-after.xml`。

旧三档的设置和榜单索引 0/1/2 迁移为新 0/2/4，权重仍为原来的 1/3/8，历史分数不重算。旧 16×16 中级局也有独立兼容布局；新设置使用固定难度 ID，榜单新版本键避免重复迁移。

## 模拟器与显示验证

设备为 `emulator-5554`，Android 9 / API 28，横屏 1920×1080。脚本保留排行榜，使用返回键暂停菜单选择难度，并单独检查 MENU / M / F1。

完整回归 141 项断言全部通过，日志：`E:\temp\TVMinesweeper\device-report-20260921-151420.txt`。覆盖五档菜单与雷数范围、四边界、返回键切换难度、默认继续与取消不换局、OK 单双击、音量键不改棋盘、首步安全、菜单/后台暂停及存档恢复。挑战局的第 19 行、第 24 列光标与旗帜在后台和重启后均保留；测试后恢复初级新局及原媒体音量。

首轮运行遇到模拟器 `uiautomator` 偶发空根节点，已为读取界面增加最多三次重试，不重复发送任何按键；操作结果仍由原断言验证。

补充验证通过：新高级与挑战棋盘顶部/底部像素均为棋格，新高级首格和第 13 行第 16 列触摸准确，八项菜单中的音效开关可切换并恢复原值、帮助可打开、继续游戏保留原棋盘。已检查 1080p 高级、挑战及旧宽棋盘，720p 挑战棋盘及八项菜单，无裁切。结束后恢复原分辨率 1080×1920 和初级空白新局。

截图直接读取 Android framebuffer，不激活或遮挡电脑其他窗口；先本地 Windows OCR，再压缩成 JPEG。原图、XML 和报告只保存在 `E:\temp\TVMinesweeper`，仓库只保存压缩图。

- [高级游戏画面](../screenshots/gameplay.jpg)
- [720p 挑战棋盘](../screenshots/challenge-720.jpg)
- [720p 五档难度菜单](../screenshots/menu-720.jpg)
- [旧 30×16 棋盘纵向铺满](../screenshots/legacy-full-height.jpg)

## 验证范围

实际运行设备为 API 28 模拟器，未连接用户的实体电视遥控器或 Android 4.3 电视。返回键难度入口继续保留，选择五档难度不依赖 MENU。API 18 兼容性由编译、兼容用法及 Lint 验证。
